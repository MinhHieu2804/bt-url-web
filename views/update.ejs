<!DOCTYPE html>
<html>
    <head>
	<meta charset="utf-8">
</head>
<body>
	<div class="output"></div>
	Mã sinh viên: <input type="text" class="masv"> <br>
	Họ và tên: <input type="text" class="hoten"> <br>
	<button class="submit">Cập nhật</button>

	<script>
		document.querySelector("button.submit").onclick = function() {
			fetch("/api/students", {
				method: "PUT",
				headers: {"Content-Type": "application/json"},
				body: '{"msv":"'+document.querySelector("input.masv").value+'","name":"'+ document.querySelector("input.hoten").value+'"}'
			})
			.then(response => response.json())
            .then(data => {
                const msg = (data.status === 200)? "Updated": "Error! Try again";
                document.querySelector("div.output").innerHTML = msg;	
		  })
		};
	</script>
</body>
</html>